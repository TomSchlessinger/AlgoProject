To run this:
open it in some IDE like IntelliJ that can use gradle, then just load the gradle build script that I've already included
you can also run the jar I attached using `java -jar terraria-bad.jar` if you want to play the game (for some reason)
note that it needs java version >=20.0.2
